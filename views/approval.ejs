<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Application Status</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: Arial, sans-serif;
        background-color: #e8e8e8;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .status-container {
        background: #f5f5f5;
        border: 2px solid #333;
        border-radius: 0;
        padding: 40px;
        max-width: 600px;
        width: 100%;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }

      .status-header {
        text-align: center;
        margin-bottom: 30px;
        border-bottom: 2px solid #333;
        padding-bottom: 20px;
      }

      .status-title {
        font-size: 28px;
        font-weight: bold;
        color: #333;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 10px;
      }

      .status-subtitle {
        font-size: 16px;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .status-content {
        margin: 30px 0;
      }

      .status-field {
        margin-bottom: 20px;
      }

      .status-field label {
        display: block;
        font-weight: bold;
        color: #333;
        margin-bottom: 8px;
        text-transform: uppercase;
        font-size: 14px;
        letter-spacing: 1px;
      }

      .status-field .value {
        background: white;
        border: 2px solid #333;
        padding: 12px 15px;
        font-size: 16px;
        color: #333;
        display: block;
        width: 100%;
        font-family: Arial, sans-serif;
      }

      .status-badge {
        display: inline-block;
        padding: 8px 16px;
        border: 2px solid;
        font-weight: bold;
        text-transform: uppercase;
        font-size: 14px;
        letter-spacing: 1px;
      }

      .badge-verified {
        background: #d4edda;
        border-color: #28a745;
        color: #155724;
      }

      .badge-pending {
        background: #fff3cd;
        border-color: #ffc107;
        color: #856404;
      }

      .badge-rejected {
        background: #f8d7da;
        border-color: #dc3545;
        color: #721c24;
      }

      .status-message {
        background: white;
        border: 2px solid #333;
        padding: 20px;
        margin: 20px 0;
        font-size: 16px;
        color: #333;
        line-height: 1.6;
      }

      .action-section {
        text-align: center;
        margin-top: 30px;
        padding-top: 20px;
        border-top: 2px solid #333;
      }

      .action-button {
        background: #333;
        color: white;
        border: 2px solid #333;
        padding: 15px 30px;
        font-size: 16px;
        font-weight: bold;
        text-transform: uppercase;
        letter-spacing: 1px;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        transition: all 0.2s ease;
        margin: 10px;
      }

      .action-button:hover {
        background: white;
        color: #333;
      }

      .btn-register {
        background: #dc3545;
        border-color: #dc3545;
      }

      .btn-register:hover {
        background: white;
        color: #dc3545;
      }

      .btn-dashboard {
        background: #28a745;
        border-color: #28a745;
      }

      .btn-dashboard:hover {
        background: white;
        color: #28a745;
      }

      .help-text {
        margin-top: 20px;
        font-size: 14px;
        color: #666;
        text-align: center;
        font-style: italic;
      }

      .pending-indicator {
        text-align: center;
        margin: 20px 0;
      }

      .pending-dots {
        display: inline-block;
        font-size: 24px;
        color: #ffc107;
        animation: blink 1.5s infinite;
      }

      @keyframes blink {
        0%,
        50% {
          opacity: 1;
        }
        51%,
        100% {
          opacity: 0.3;
        }
      }

      @media (max-width: 600px) {
        .status-container {
          padding: 20px;
          margin: 10px;
        }

        .status-title {
          font-size: 20px;
        }

        .action-button {
          width: 100%;
          margin: 5px 0;
        }
      }
    </style>
  </head>
  <body>
    <div class="status-container">
      <div class="status-header">
        <h1 class="status-title">Application Status</h1>
        <p class="status-subtitle">Government Services Portal</p>
      </div>

      <div class="status-content">
        <% if (user && user.approvalLevel === 1) { %>
        <!-- VERIFIED STATUS -->
        <div class="status-field">
          <label>Current Status</label>
          <span class="status-badge badge-verified">✓ Approved</span>
        </div>

        <div class="status-field">
          <label>Full Name</label>
          <div class="value"><%= user.firstName %> <%= user.lastName %></div>
        </div>

        <div class="status-field">
          <label>User ID</label>
          <div class="value"><%= user.userId || user._id %></div>
        </div>

        <div class="status-field">
          <label>Approval Date</label>
          <div class="value"><%= new Date().toLocaleDateString() %></div>
        </div>

        <div class="status-message">
          <strong>Congratulations!</strong> Your application has been
          successfully verified and approved. You can now access all platform
          features and services.
        </div>

        <% } else if (user && user.approvalLevel === 0) { %>
        <!-- PENDING STATUS -->
        <div class="status-field">
          <label>Current Status</label>
          <span class="status-badge badge-pending">⏳ Under Review</span>
        </div>

        <div class="status-field">
          <label>Full Name</label>
          <div class="value"><%= user.firstName %> <%= user.lastName %></div>
        </div>

        <div class="status-field">
          <label>User ID</label>
          <div class="value"><%= user.userId || user._id %></div>
        </div>

        <div class="status-field">
          <label>Submitted Date</label>
          <div class="value"><%= new Date().toLocaleDateString() %></div>
        </div>

        <div class="pending-indicator">
          <div class="pending-dots">● ● ●</div>
        </div>

        <div class="status-message">
          Your application is currently being reviewed by our verification team.
          This process typically takes 24-48 hours. Please check back later for
          updates.
        </div>

        <% } else if (user && user.approvalLevel === -1) { %>
        <!-- REJECTED STATUS -->
        <div class="status-field">
          <label>Current Status</label>
          <span class="status-badge badge-rejected">✗ Rejected</span>
        </div>

        <div class="status-field">
          <label>Full Name</label>
          <div class="value"><%= user.firstName %> <%= user.lastName %></div>
        </div>

        <div class="status-field">
          <label>User ID</label>
          <div class="value"><%= user.userId || user._id %></div>
        </div>

        <div class="status-field">
          <label>Review Date</label>
          <div class="value"><%= new Date().toLocaleDateString() %></div>
        </div>

        <div class="status-message">
          <strong>Application Rejected:</strong> Unfortunately, your application
          could not be approved at this time. Please review the requirements
          carefully and submit a new application with proper documentation.
        </div>

        <% } else { %>
        <!-- UNKNOWN STATUS -->
        <div class="status-field">
          <label>Current Status</label>
          <span class="status-badge badge-pending">? Unknown</span>
        </div>

        <div class="status-message">
          We couldn't determine your application status. Please contact support
          for assistance or submit a new application.
        </div>
        <% } %>
      </div>

      <div class="action-section">
        <% if (user && user.approvalLevel === 1) { %>
        <a href="/dashboard" class="action-button btn-dashboard"
          >Go to Dashboard</a
        >
        <% } else if (user && user.approvalLevel === 0) { %>
        <button class="action-button" onclick="window.location.reload()">
          Check Status Again
        </button>
        <% } else if (user && user.approvalLevel === -1) { %>
        <a href="/register" class="action-button btn-register"
          >Submit New Application</a
        >
        <% } else { %>
        <a href="/register" class="action-button">Submit Application</a>
        <% } %>

        <div class="help-text">
          <% if (user && user.approvalLevel === 1) { %> Welcome to the platform!
          You're all set to get started. <% } else if (user &&
          user.approvalLevel === 0) { %> We'll notify you once the review is
          complete. <% } else if (user && user.approvalLevel === -1) { %> Make
          sure all documents are clear and meet requirements. <% } else { %> If
          you believe this is an error, please contact support. <% } %>
        </div>
      </div>
    </div>

    <script>
      // Auto-refresh for pending status every 30 seconds
      if (user && user.approvalLevel === 0) {
        setTimeout(() => {
          window.location.reload();
        }, 30000);
      }
    </script>
  </body>
</html>

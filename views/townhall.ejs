<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Townhall Discussion - Government Portal</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: Arial, sans-serif;
        background-color: #e8e8e8;
        height: 100vh;
        overflow: hidden;
      }

      .chat-container {
        height: 100vh;
        display: flex;
        flex-direction: column;
        background: #e8e8e8;
      }

      /* Header */
      .chat-header {
        background: #f5f5f5;
        border: 2px solid #333;
        border-bottom: none;
        padding: 20px;
        text-align: center;
      }

      .chat-title {
        font-size: 28px;
        font-weight: bold;
        color: #333;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 5px;
      }

      .chat-subtitle {
        font-size: 14px;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      /* Messages Area */
      .chat-messages {
        flex: 1;
        overflow-y: auto;
        background: #f5f5f5;
        border: 2px solid #333;
        border-top: none;
        border-bottom: none;
        padding: 20px;
      }

      .message-container {
        max-width: 800px;
        margin: 0 auto;
      }

      .chat-message {
        margin-bottom: 15px;
        background: white;
        border: 2px solid #333;
        padding: 15px;
      }

      .message-header {
        background: #333;
        color: white;
        padding: 8px 12px;
        margin: -15px -15px 10px -15px;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .message-user {
        font-weight: bold;
        text-transform: uppercase;
        font-size: 12px;
        letter-spacing: 1px;
      }

      .message-time {
        font-size: 11px;
        opacity: 0.8;
      }

      .message-text {
        color: #333;
        line-height: 1.5;
        font-size: 14px;
      }

      /* Input Area */
      .chat-input-area {
        background: #f5f5f5;
        border: 2px solid #333;
        padding: 20px;
      }

      .input-section-header {
        background: #333;
        color: white;
        padding: 10px 15px;
        margin: -20px -20px 15px -20px;
        text-align: center;
      }

      .input-section-title {
        font-size: 16px;
        font-weight: bold;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .chat-form {
        max-width: 800px;
        margin: 0 auto;
      }

      .input-container {
        display: flex;
        gap: 10px;
        align-items: stretch;
      }

      .chat-input {
        flex: 1;
        border: 2px solid #333;
        padding: 12px 15px;
        font-size: 14px;
        color: #333;
        background: white;
        font-family: Arial, sans-serif;
      }

      .chat-input:focus {
        outline: none;
        background: #f9f9f9;
      }

      .chat-input::placeholder {
        color: #666;
        text-transform: uppercase;
        font-size: 12px;
        letter-spacing: 1px;
      }

      .send-button {
        background: #333;
        color: white;
        border: 2px solid #333;
        padding: 12px 25px;
        font-size: 14px;
        font-weight: bold;
        text-transform: uppercase;
        letter-spacing: 1px;
        cursor: pointer;
        transition: all 0.2s ease;
      }

      .send-button:hover {
        background: white;
        color: #333;
      }

      .send-button:disabled {
        opacity: 0.6;
        cursor: not-allowed;
      }

      /* Status Bar */
      .chat-status {
        background: #333;
        color: white;
        padding: 10px 20px;
        text-align: center;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .status-user {
        color: #f5f5f5;
        font-weight: bold;
      }

      /* Scrollbar styling */
      .chat-messages::-webkit-scrollbar {
        width: 8px;
      }

      .chat-messages::-webkit-scrollbar-track {
        background: #f5f5f5;
        border: 1px solid #333;
      }

      .chat-messages::-webkit-scrollbar-thumb {
        background: #333;
      }

      .chat-messages::-webkit-scrollbar-thumb:hover {
        background: #555;
      }

      /* User info section */
      .user-info {
        background: white;
        border: 2px solid #333;
        padding: 10px 15px;
        margin-bottom: 15px;
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
        margin-bottom: 15px;
      }

      .user-info-header {
        background: #333;
        color: white;
        padding: 5px 10px;
        margin: -10px -15px 8px -15px;
        font-size: 12px;
        font-weight: bold;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .user-info-content {
        font-size: 12px;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      /* Responsive design */
      @media (max-width: 768px) {
        .chat-header {
          padding: 15px;
        }

        .chat-title {
          font-size: 20px;
        }

        .chat-messages {
          padding: 15px;
        }

        .input-container {
          flex-direction: column;
        }

        .send-button {
          width: 100%;
        }
      }

      /* Animation for new messages */
      @keyframes newMessage {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .chat-message.new {
        animation: newMessage 0.3s ease-out;
      }
    </style>
  </head>
  <body>
    <div class="chat-container">
      <!-- Header -->
      <div class="chat-header">
        <h1 class="chat-title">Townhall Discussion</h1>
        <div class="chat-subtitle">
          Government Services Portal - Community Forum
        </div>
      </div>

      <!-- User Info -->
      <div class="user-info">
        <div class="user-info-header">Session Information</div>
        <div class="user-info-content">
          Connected as: <strong><%= username %></strong> | Session Active |
          Community Discussion Forum
        </div>
      </div>

      <!-- Messages Area -->
      <div class="chat-messages" id="chat-messages">
        <div class="message-container">
          <% messages.forEach(function(msg) { %>
          <div class="chat-message">
            <div class="message-header">
              <span class="message-user"><%= msg.username %></span>
              <span class="message-time"><%= msg.time %></span>
            </div>
            <div class="message-text"><%= msg.text %></div>
          </div>
          <% }) %>
        </div>
      </div>

      <!-- Input Area -->
      <div class="chat-input-area">
        <div class="input-section-header">
          <div class="input-section-title">Submit Message</div>
        </div>
        <form
          class="chat-form"
          action="/townhall/message"
          method="POST"
          autocomplete="off"
        >
          <div class="input-container">
            <input
              type="text"
              name="text"
              class="chat-input"
              placeholder="Enter your message here..."
              required
            />
            <button type="submit" class="send-button">Send Message</button>
          </div>
        </form>
      </div>

      <!-- Status Bar -->
      <div class="chat-status">
        Government Portal | User:
        <span class="status-user"><%= username %></span> | Community Discussion
        Active
      </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/townhall.js"></script>
    <script>
      // Auto-scroll to bottom on page load
      document.addEventListener("DOMContentLoaded", function () {
        const chatMessages = document.getElementById("chat-messages");
        if (chatMessages) {
          chatMessages.scrollTop = chatMessages.scrollHeight;
        }
      });

      // Auto-scroll on new messages
      const chatMessages = document.getElementById("chat-messages");
      if (chatMessages) {
        const observer = new MutationObserver(function (mutations) {
          mutations.forEach(function (mutation) {
            if (mutation.addedNodes.length > 0) {
              // Add animation class to new messages
              mutation.addedNodes.forEach(function (node) {
                if (
                  node.nodeType === 1 &&
                  node.classList.contains("chat-message")
                ) {
                  node.classList.add("new");
                }
              });
            }
          });
          chatMessages.scrollTop = chatMessages.scrollHeight;
        });
        observer.observe(chatMessages, { childList: true, subtree: true });
      }

      // Enhanced form submission - let Socket.IO handle the UI updates
      document
        .querySelector(".chat-form")
        .addEventListener("submit", function (e) {
          const input = this.querySelector(".chat-input");
          if (!input.value.trim()) {
            e.preventDefault();
            return;
          }

          // Add loading state
          const button = this.querySelector(".send-button");
          const originalText = button.textContent;
          button.textContent = "SENDING...";
          button.disabled = true;

          // Reset button state after a short delay
          setTimeout(() => {
            button.textContent = originalText;
            button.disabled = false;
          }, 1000);
        });

      // Keyboard shortcuts
      document.addEventListener("keydown", function (event) {
        if (event.key === "Enter" && event.ctrlKey) {
          document
            .querySelector(".chat-form")
            .dispatchEvent(new Event("submit"));
        }
      });
    </script>
  </body>
</html>

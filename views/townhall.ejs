<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Townhall Chat - UPM</title>
    <link rel="stylesheet" href="/css/globals.css">
    <link rel="stylesheet" href="/css/newpg.css">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      body {
        background: #f7e8d9;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        height: 100vh;
        overflow: hidden;
      }
      
      .chat-container {
        height: 100vh;
        display: flex;
        flex-direction: column;
        background: #f7e8d9;
      }
      
      /* Header matching dashboard style */
      .chat-header {
        background: linear-gradient(135deg, #2d1810 0%, #1a0f0a 100%);
        color: white;
        padding: 20px 24px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        box-shadow: 0 2px 20px rgba(0,0,0,0.15);
        position: relative;
        z-index: 10;
      }
      
      .chat-header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, transparent 100%);
        pointer-events: none;
      }
      
      .header-left {
        display: flex;
        align-items: center;
        gap: 16px;
      }
      
      .chat-title {
        font-size: 1.75rem;
        font-weight: 700;
        margin: 0;
        letter-spacing: -0.02em;
      }
      
      .chat-subtitle {
        font-size: 0.9rem;
        opacity: 0.8;
        margin-top: 2px;
      }
      
      .header-badge {
        background: rgba(247, 232, 217, 0.2);
        color: #f7e8d9;
        padding: 6px 14px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
        border: 1px solid rgba(247, 232, 217, 0.3);
      }
      
      /* Chat messages area */
      .chat-messages {
        flex: 1;
        overflow-y: auto;
        padding: 20px;
        background: #f7e8d9;
        position: relative;
      }
      
      .chat-messages::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: 
          radial-gradient(circle at 25% 25%, rgba(139, 69, 19, 0.03) 0%, transparent 25%),
          radial-gradient(circle at 75% 75%, rgba(160, 82, 45, 0.02) 0%, transparent 25%);
        pointer-events: none;
        z-index: 1;
      }
      
      .message-container {
        position: relative;
        z-index: 2;
      }
      
      .chat-message {
        margin-bottom: 12px;
        display: flex;
        flex-direction: column;
      }
      
      .message-bubble {
        background: white;
        border-radius: 18px 18px 18px 6px;
        padding: 12px 16px;
        max-width: 75%;
        box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        border: 1px solid rgba(139, 69, 19, 0.1);
        position: relative;
        animation: slideInMessage 0.3s ease-out;
      }
      
      @keyframes slideInMessage {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      
      .message-bubble::before {
        content: '';
        position: absolute;
        bottom: 0;
        left: -6px;
        width: 0;
        height: 0;
        border: 6px solid transparent;
        border-right-color: white;
        border-bottom-color: white;
        border-left: none;
        border-top: none;
      }
      
      .message-header {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 6px;
      }
      
      .message-user {
        font-weight: 600;
        color: #8B4513;
        font-size: 0.9rem;
      }
      
      .message-time {
        color: #A0522D;
        font-size: 0.75rem;
        opacity: 0.8;
      }
      
      .message-text {
        color: #2d1810;
        line-height: 1.4;
        font-size: 0.95rem;
      }
      
      /* Input area */
      .chat-input-area {
        background: white;
        padding: 16px 20px;
        border-top: 1px solid rgba(139, 69, 19, 0.15);
        box-shadow: 0 -2px 20px rgba(0,0,0,0.1);
      }
      
      .input-container {
        display: flex;
        align-items: center;
        gap: 12px;
        background: #f7e8d9;
        border-radius: 25px;
        padding: 8px;
        border: 2px solid rgba(139, 69, 19, 0.1);
        transition: all 0.3s ease;
      }
      
      .input-container:focus-within {
        border-color: #8B4513;
        box-shadow: 0 0 0 3px rgba(139, 69, 19, 0.1);
      }
      
      .chat-input {
        flex: 1;
        border: none;
        outline: none;
        background: transparent;
        padding: 12px 16px;
        font-size: 1rem;
        color: #2d1810;
        font-family: inherit;
      }
      
      .chat-input::placeholder {
        color: #A0522D;
        opacity: 0.6;
      }
      
      .send-button {
        width: 44px;
        height: 44px;
        border: none;
        background: linear-gradient(135deg, #8B4513 0%, #A0522D 100%);
        color: white;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        transition: all 0.3s ease;
        box-shadow: 0 2px 8px rgba(139, 69, 19, 0.3);
      }
      
      .send-button:hover {
        transform: scale(1.05);
        box-shadow: 0 4px 12px rgba(139, 69, 19, 0.4);
      }
      
      .send-button:active {
        transform: scale(0.95);
      }
      
      /* Status bar */
      .chat-status {
        background: linear-gradient(90deg, rgba(139, 69, 19, 0.05) 0%, rgba(160, 82, 45, 0.05) 100%);
        padding: 8px 20px;
        text-align: center;
        font-size: 0.85rem;
        color: #8B4513;
        border-top: 1px solid rgba(139, 69, 19, 0.1);
      }
      
      /* Scrollbar styling */
      .chat-messages::-webkit-scrollbar {
        width: 6px;
      }
      
      .chat-messages::-webkit-scrollbar-track {
        background: transparent;
      }
      
      .chat-messages::-webkit-scrollbar-thumb {
        background: rgba(139, 69, 19, 0.2);
        border-radius: 3px;
      }
      
      .chat-messages::-webkit-scrollbar-thumb:hover {
        background: rgba(139, 69, 19, 0.3);
      }
      
      /* Responsive design */
      @media (max-width: 768px) {
        .chat-header {
          padding: 16px 20px;
        }
        .chat-title {
          font-size: 1.5rem;
        }
        .message-bubble {
          max-width: 85%;
        }
        .chat-input-area {
          padding: 12px 16px;
        }
      }
    </style>
</head>
<body>
    <div class="chat-container">
      <!-- Header -->
      <div class="chat-header">
        <div class="header-left">
          <div>
            <h1 class="chat-title">Townhall</h1>
            <div class="chat-subtitle">Community Discussion</div>
          </div>
        </div>
        <div class="header-badge">Chat</div>
      </div>
      
      <!-- Messages Area -->
      <div class="chat-messages" id="chat-messages">
        <div class="message-container">
          <% messages.forEach(function(msg) { %>
            <div class="chat-message">
              <div class="message-bubble">
                <div class="message-header">
                  <span class="message-user"><%= msg.username %></span>
                  <span class="message-time"><%= msg.time %></span>
                </div>
                <div class="message-text"><%= msg.text %></div>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
      
      <!-- Input Area -->
      <div class="chat-input-area">
        <form class="chat-form" action="/townhall/message" method="POST" autocomplete="off">
          <div class="input-container">
            <input type="text" name="text" class="chat-input" placeholder="Type your message..." required />
            <button type="submit" class="send-button">
              <span>➤</span>
            </button>
          </div>
        </form>
      </div>
      
      <!-- Status Bar -->
      <div class="chat-status">
        Connected as <strong><%= username %></strong> • Community Chat
      </div>
    </div>
    
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/townhall.js"></script>
    <script>
      // Auto-scroll to bottom on page load
      document.addEventListener('DOMContentLoaded', function() {
        const chatMessages = document.getElementById('chat-messages');
        if (chatMessages) {
          chatMessages.scrollTop = chatMessages.scrollHeight;
        }
      });
      
      // Auto-scroll on new messages
      const chatMessages = document.getElementById('chat-messages');
      if (chatMessages) {
        const observer = new MutationObserver(function() {
          chatMessages.scrollTop = chatMessages.scrollHeight;
        });
        observer.observe(chatMessages, { childList: true, subtree: true });
      }
      
      // Enhanced form submission
      document.querySelector('.chat-form').addEventListener('submit', function(e) {
        const input = this.querySelector('.chat-input');
        if (!input.value.trim()) {
          e.preventDefault();
          return;
        }
        
        // Add loading state
        const button = this.querySelector('.send-button');
        button.style.opacity = '0.6';
        button.disabled = true;
        
        setTimeout(() => {
          button.style.opacity = '1';
          button.disabled = false;
        }, 1000);
      });
    </script>
</body>
</html>
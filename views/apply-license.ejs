<!DOCTYPE html>
<html>
<head>
    <title>Apply for License</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f7e8d9; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 8px; box-shadow: 0 2px 8px #ccc; padding: 32px; }
        h1 { text-align: center; }
        form { display: flex; flex-direction: column; gap: 14px; }
        label { font-weight: bold; }
        input, select { padding: 8px; border-radius: 4px; border: 1px solid #bbb; font-size: 1rem; }
        button { padding: 10px 18px; background: #4a90e2; color: #fff; border: none; border-radius: 4px; font-size: 1rem; cursor: pointer; }
        button:hover { background: #357ab8; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Apply for License</h1>
        <form action="/apply-license" method="POST">
            <label for="citizenId">Citizen ID</label>
            <input type="text" id="citizenId" name="citizenId" required />
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required />
            <label for="dob">Date of Birth</label>
            <input type="date" id="dob" name="dob" required />
            <label for="gender">Gender</label>
            <select id="gender" name="gender" required>
                <option value="Male">Male</option>
                <option value="Female">Female</option>
                <option value="Other">Other</option>
            </select>
            <label for="address">Address</label>
            <input type="text" id="address" name="address" required />
            <label for="phone">Phone</label>
            <input type="text" id="phone" name="phone" required />
            <label for="licenseType">License Type</label>
            <input type="text" id="licenseType" name="licenseType" required />
            <label for="bloodGroup">Blood Group</label>
            <input type="text" id="bloodGroup" name="bloodGroup" required />
            <label for="docRefs">Document References (comma separated IDs)</label>
            <input type="text" id="docRefs" name="docRefs" />
            <button type="submit">Submit Application</button>
        </form>
    </div>
</body>
</html>

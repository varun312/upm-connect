<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document Vault</title>
    <link rel="stylesheet" href="css/globals.css" />
    <link rel="stylesheet" href="css/login.css" />
    <style>
        /* Additional vault-specific styles if needed */
        body { font-family: Arial, sans-serif; background: #f7e8d9; margin: 0; padding: 0; }
        .container { max-width: 700px; margin: 40px auto; border-radius: 8px; padding: 3px; height: fit-content;}
        .main-content { 
            padding: 20px; 
            margin-top: 50px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 400px;
        }
        h1 { text-align: center; }
        form { display: flex; flex-direction: column; gap: 14px; margin-bottom: 3px; }
        label { font-weight: bold; }
        input, select { padding: 8px; border-radius: 4px; border: 1px solid #bbb; font-size: 1rem; }
        button { padding: 10px 18px; background: #4a90e2; color: #fff; border: none; border-radius: 4px; font-size: 1rem; cursor: pointer; }
        button:hover { background: #357ab8; }
        table { width: 100%; border-collapse: collapse; margin-top: 24px; }
        th, td { padding: 10px 8px; border-bottom: 1px solid #ddd; text-align: left; }
        th { background: #f0e6d2; }
        tr:last-child td { border-bottom: none; }
        .actions { display: flex; gap: 10px; }
        .ref-btn { background: #826e5a; }
        .ref-btn:hover { background: #6d594a; }
        .dl-btn { background: #0a2342; }
        .dl-btn:hover { background: #16335a; }
    </style>
</head>
<body>
    <div class="container">       
        <!-- Main Content -->
        <main class="main-content">
            <div class="form-container">
                <h1 class="form-title">Document Vault</h1>
                <form action="/vault/upload" method="POST" enctype="multipart/form-data" class="login-form">
                    <!-- Document Type -->
                    <div class="form-group">
                        <label for="docType">Document Type</label>
                        <input type="text" id="docType" name="docType" class="form-input" />
                    </div>
                    <!-- Source -->
                    <div class="form-group">
                        <label for="source">Source</label>
                        <select id="source" name="source" class="form-input" required>
                            <option value="service-center">Service Center</option>
                            <option value="scanner">Scanner</option>
                            <option value="mail-in">Mail-in</option>
                        </select>
                    </div>
                    <!-- File -->
                    <div class="form-group">
                        <label for="file">File</label>
                        <input type="file" id="file" name="file" class="form-input" required />
                    </div>
                    <!-- Action Buttons -->
                    <div class="form-actions">
                        <button type="submit" class="btn-primary">Upload Document</button>
                    </div>
                </form>
            </div>
        </main>
    </div>
</body>
</html>
